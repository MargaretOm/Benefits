import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#efefef',
    paddingVertical: 10,
    marginHorizontal: 5,
    borderRadius: 10,
    width: 120,
    alignItems: 'center',
  },
  activeContainer: {
    backgroundColor: '#2948cc',
  },
  activeText: {
    color: 'white',
    height: 18,
  },
});
